//import { ConfigHandler } from "ucbuilder/out/main/configs/ConfigHandler.js";

export class start {
    static building(pth: string) {
       // const handler = new ConfigHandler();
       // handler.fill(pth);
        console.log('her');
        
    }
}